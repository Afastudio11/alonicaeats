# Production Environment Variables Template
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration (Required)
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Session Security (Required - Generate a random string)
SESSION_SECRET=your-super-secret-session-key-change-this

# Google Cloud Storage Configuration (Optional - only if using file uploads)
# Get these from Google Cloud Console
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GCS_BUCKET=your-bucket-name

# Or use JSON credentials directly in production
GOOGLE_CLOUD_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Admin User Setup (REQUIRED - for initial admin creation)
# Set a strong password - minimum 8 characters required
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=

# Midtrans Payment Gateway (REQUIRED for QRIS payments)
# Get these from Midtrans Dashboard: https://dashboard.midtrans.com/
MIDTRANS_SERVER_KEY=SB-Mid-server-YOUR_SERVER_KEY_HERE
MIDTRANS_CLIENT_KEY=SB-Mid-client-YOUR_CLIENT_KEY_HERE

# Replit Production Variables (if deploying elsewhere, these can be ignored)
# REPLIT_DB_URL=your-replit-db-url